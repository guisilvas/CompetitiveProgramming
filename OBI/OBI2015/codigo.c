#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, num, x, y, z, cont=0;
    scanf("%d",&n);
    for(int i=0;i<(n*2-1);++i)
    {
        if(i%2==0)
        {
            scanf("%d",&num);
            if(num==1)
            {
                x==1;
            }
            if((num==0)&&(x==1))
            {
                y==0;
            }
            else
            {
                x==
            }
            if((num==0)&&(x==1)&&(y==0))
            {
                z==0;
            }
            if((x==1)&&(y==0)&&(z==0))
            {
                cont++;
            }
            x=2,y=2,z=2;
        }
    }
    printf("%d",cont);
    return 0;
}